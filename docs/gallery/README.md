# Web UI gallery

These 1280 × 720 screenshots show the real bundled Web UI using synthetic
sessions from `scripts/webui/fixture-server.mjs`, captured at revision `9a1f304`.
They contain no provider credentials or private session data. The fixture's
example results are presentation data, not evidence of an actual agent run.

To refresh, run `node scripts/webui/fixture-server.mjs` and open the printed URL.
Capture the new-session workspace, then open “Build a better workspace” with
the session rail visible. Capture the Now inspector, Results → diff, and the
command palette. Keep image filenames and dimensions consistent with the
landing page, and update this revision when refreshing the captures.
