// WebUI entry module. The client is split into ES modules under ./js/
// (loaded natively, no bundler); main.js runs the init sequence and every
// feature module self-registers its listeners.
import './js/main.js';
